with (obj_player)
{
	if state = states.tumble or state = states.knightpepslopes or state = states.mach3 or state = states.mach2
	{
other.image_speed = 0.35
if xscale != other.image_xscale
xscale = other.image_xscale

	}
}

